global:
  scrape_interval: 10s


scrape_configs:
  - job_name: "traefik"
    metrics_path: /metrics
    scheme:  https
    basic_auth:
      username: admin
      password: Password123
    tls_config:
      insecure_skip_verify: true
    static_configs:
      - targets:
        - traefik.local
